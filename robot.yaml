tasks:
  Run all tasks:
    shell: python -m robot --report NONE --outputdir output --logtitle "Log de Tarefas" tasks.robot

condaConfigFile: conda.yaml
artifactsDir: output
PATH:
  - .
PYTHONPATH:
  - .
ignoreFiles:
  - .gitignore
